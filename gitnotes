// config
git config --list
git config --global user.name "jpechacek80"
git config --global user.email "jpechacek@yahoo.com"

// init <directory>   //create local repo
>git init
>git add .            //from exiting src
>git commit
// edit a file
>git push             //pushes changes


// clone <repo> <directory>  //calls on git init
>git clone ssh://john@example.com/path/my-project.git
>git clone git@github.com:jpechacek80/testmake.git 



git add -A
git commit -m "comment"

git clone git@github.com:jpechacek80/testmake.git 

git remote add origin git@github.com:jpechacek80/testmake.git

git push -u origin master
