//git-reset

reset  <tag|HEAD~n> foo.c  //no change to HEAD
reset <branch|HEAD~n>    //moves HEAD
options:    -=hard|--soft

git reset
  -unstaging a file
  -move the branch HEAD
  -Return Wdir to previous commit
git revert
  -writes new commit on branch tip
  -undoes a specific commit
  -overwrite Wdir

git reset --hard #hash
git stash push/pop
